@startuml

class Connect4
class GameView
class YesNoDialog
class Board
class BoardView
class Turn
class TurnView
class Coordenate


Connect4 *-- YesNoDialog
Connect4 *-- GameView
GameView *-- BoardView
BoardView *-- TurnView
BoardView ..> Board
Board *-- Turn
Board *-- Coordenate
TurnView ..> Turn

Connect4 : +start()

YesNoDialog : -question
YesNoDialog : -answer
YesNoDialog : -saveAnswer()
YesNoDialog : +isAffirmative()
YesNoDialog : +isNegative()
YesNoDialog : +askToPlayAgain()



Turn : -MAX_PLAYERS
Turn : -turn
Turn : -tokens
Turn : -tokensName
Turn : +getToken()
Turn : +getTokenName()
Turn : +next()

TurnView : +printTurn()
TurnView : +getToken()
TurnView : +getTokenName()
TurnView : +next()

GameView : -boardView{BoardView}
GameView : +printTitle()
GameView : +printWinnerMsg()
GameView : +printLosserMsg()
GameView : +printTieMsg()
GameView : +printRoundResults()
GameView : +play()
GameView : +resume()


Board : -COLUMNS
Board : -ROWS
Board : -TOKEN_EMPTY
Board : -CONNECTIONS_TO_GOAL
Board : -DIRECTIONS_TO_GOAL[]
Board : +turn {Turn}
Board : +board[]
Board : +isCompleted()
Board : +addtokenToBoard()
Board : +columnInRange()
Board : +columnCompleted()
Board : +dropToken(token)
Board : +anyGoalAchived()



Coordenate : +column
Coordenate : +row 
Coordenate : +direction



BoardView : +turn{TurnView}
BoardView : -askValidColumnNumber()
BoardView : -columnCompleted()
BoardView : -printError()
BoardView : +printBoard()
BoardView : +askValidColumn()
BoardView : +boardCompleted()
BoardView : +anyGoalAchived()
BoardView : +addToken()
@enduml


